# Recommended Python version: 3.11.x
# Python 3.13 is not recommended for this project because SHAP may fall back to a source build on Windows.
Flask==3.1.0
# Imported directly for password hashing, upload handling, and HTTP errors.
Werkzeug==3.1.3
joblib==1.4.2
lightgbm>=4.0,<5.0
numpy==2.2.3
pandas==2.2.3
scipy==1.15.2
scikit-learn==1.6.1
shap==0.46.0
waitress==3.0.2
pymodbus>=3.8,<4.0
openpyxl>=3.1,<4.0
